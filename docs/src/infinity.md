```@meta
CurrentModule = MobiusTransformations
```

# set_infinity

The value a transformation returns at a pole (where the denominator vanishes)
is configurable, defaulting to `complex(Inf)`.

```julia
set_infinity(Inf)   # the default
```

```@docs
set_infinity
```
